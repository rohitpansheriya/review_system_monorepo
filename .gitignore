# Node / Firebase
node_modules/
.firebase/
firebase-debug.log*
firestore-debug.log*

# Flutter / Dart
.dart_tool/
.build/
build/
*.flutter-plugins
*.flutter-plugins-dependencies

# Mac OS
.DS_Store

# IDE
.idea/
.vscode/

# ── Secrets & generated config (NEVER COMMIT) ────────────────
# Real Firebase env vars — copy .env.example → .env to get started
.env
.env.local
.env.*.local

# Service account key files (downloaded from Firebase console)
# Required locally for scripts that write to production Firestore.
# NEVER commit these — they grant full admin access to the project.
*firebase-adminsdk*.json

# Generated by scripts/inject-firebase-config.js from .env
review_page/firebase-config.js

# Flutter app — downloaded from Firebase console per platform
admin_panel/android/app/google-services.json
admin_panel/ios/Runner/GoogleService-Info.plist
functions/.secret.local
*serviceAccount*.json
functions/.env*
!functions/.env.example
serviceAccountKey.json
*firebase-adminsdk*.json
